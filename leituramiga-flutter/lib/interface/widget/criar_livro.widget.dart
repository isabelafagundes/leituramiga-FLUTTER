import 'package:flutter/material.dart';

class CriarLivroWidget extends StatelessWidget {
  const CriarLivroWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
